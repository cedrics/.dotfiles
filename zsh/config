autoload -U colors && colors

export EDITOR="mvim -v"

autoload -U ~/.dotfiles/zsh/functions/*(:t)

PS1="%{$fg[magenta]%}%c%{$reset_color%} %{$fg[yellow]%}%{$reset_color%} ➝ "

"/Users/sohrauer/.rvm/scripts/rvm" ]] && source "/Users/sohrauer/.rvm/scripts/rvm"  # This loads RVM into a shell session.
